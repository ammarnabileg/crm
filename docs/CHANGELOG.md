# Changelog

All notable changes to **HalaOps** are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

This file is part of the `/docs` single source of truth. See [00-README](00-README.md)
for how the documentation set is organized and the documentation-first development rule.

## [Unreleased]

### Added
- **Continuous Development Protocol adopted ([54-Continuous-Development-Protocol](54-Continuous-Development-Protocol.md)).** A binding, standing governance contract for *every* future change — before, during and after: read-before-modify + impact analysis, minimal change, extend-don't-rewrite, no-duplication, document-first, regression-always, no dead code, consistency, root-cause fixes, keep-clean, and final validation — the goal being to make the system *better, not bigger*. Captured concisely **on top of (not duplicating)** the existing [49 Development Workflow](49-Development-Workflow.md) and [50 Continuous Project Audit](50-Continuous-Project-Audit.md) bibles, which it cross-references for the detailed mechanics; indexed in [00-README](00-README.md) (now 54 numbered documents). No code/schema change.
- **Final Enterprise Audit & Release pass (Phase 14) — whole-project review, all green.** No new features — four parallel read-only audits (routes/nav integrity, RBAC, dead code, docs↔code drift) + deterministic verification (full suite, 403-file lint, real-browser smoke across every page). Outcome: every route/nav link resolves (no broken routes or dead buttons); no duplicate or undefined-in-code permissions. **Concrete fixes:** added the missing **`workspace.delete`** permission (it backed `WorkspacePolicy::delete` but wasn't in the catalogue) + idempotent sync migration `0047`; removed 2 unused imports (`AgentRunner`, `InstallManager`); exercised the previously-orphan **`card`** component in the `/design` catalog; and corrected stale docs — `00-README` doc count (47→53) and recruitment now listed as built; `33-System-Diagnostics` non-existent paths/routes (`platform/*`→`system/*`) and dropped the "planned" tag on `queued_jobs`/`failed_jobs`; `35-Performance` `platform.diagnostics`→`system.manage`; `30` nav-registry example refreshed to the real registry. **Verified:** full suite **271 passing / 869 assertions / 0 failures**, browser smoke **0 console / 0 network errors**, fresh-from-empty-DB build clean. The 8-service "service-layer-done, UI-later" cluster (AdvancedSearch, TalentPool, Learning, Simulation, Benchmark, CostOptimizer, Memory, WorkflowRuntime) is tested + documented future-integration, retained intentionally (not dead code).
- **Production Readiness pass (Phase 13) — health checks, liveness probe, from-scratch deploy verified.** Confirmed an Enterprise-grade, deploy-today posture. **No-debug audit:** `APP_DEBUG` defaults false, generic error pages (no stack traces/SQL/paths), and zero `var_dump`/`dd`/`console.log`/debug routes/test data/secrets in shipped code. **System Status / Health report** (`/system/diagnostics`, super-admin) extended to cover *every* Bible category — added **Queue** (`queued_jobs` backlog + `failed_jobs`) and **Scheduler** (active tasks + cron last-run heartbeat) checks alongside Database / Storage / Cache / Mail / AI / Environment-Security. Added an unauthenticated **`GET /up` liveness probe** (JSON DB-ping, maintenance-bypassing, leaks nothing) for load balancers / uptime monitors. **Fixed a real deploy gap:** the private `storage/app/{uploads,exports,imports}` trees didn't ship (git drops empty dirs), so uploads would fail on a fresh clone — added `.gitkeep` + a precise `.gitignore` rule (the dirs are tracked, uploaded files stay ignored); the storage health check now passes. **Self-deployment test (gold standard):** built the entire schema into a brand-new empty database — all migrations + seeders, **206 tables / 552 FKs / 0 ENUMs / permissions incl. recruitment, zero errors** — then dropped it; with the register→hire `AcceptanceJourneyTest` this proves a clean install end to end with no manual/CLI steps. Locked by `tests/Feature/ProductionReadinessTest`. **Full suite 271 passing / 868 assertions.** See [33-System-Diagnostics](33-System-Diagnostics.md).
- **Performance & Scalability pass (Phase 12) — N+1 fix + bounded loads, index-verified.** Audited the hot paths for 100k-companies / 100M-rows scale. The schema already carries the right composite indexes — `EXPLAIN` confirms the Jobs list uses `jobs_workspace_created_index`, the per-job application counts use `uq_applications_workspace_job_user`, and the board uses `ix_applications_workspace_applied_at` (all index-backed, zero full scans). Fixed two real issues: **(1) N+1 on the Jobs list** — it counted applications with one query *per job* (up to 100); now a single grouped `COUNT(*) … GROUP BY job_id`. **(2) Unbounded Kanban board** — it loaded *every* application for a job into memory; now a capped, indexed, most-recent slice (500) with a visible "showing the first N — filter for more" notice (no silent truncation). Added a lightweight DB query counter (`Database::getQueryCount()/resetQueryCount()`) and `tests/Feature/PerformanceTest` proving the Jobs list issues a **constant** query count as jobs grow (N+1 regression guard), the board load is bounded, and the hot tables keep their `workspace_id`-leading + RBAC composite indexes. No redesign — incremental, backward-compatible. **Full suite 267 passing / 795 assertions.** See [35-Performance §Business Rules](35-Performance.md).
- **Security & Hardening pass (Phase 11) — audit + fixes, browser-verified.** Audited every Security Bible dimension. Already solid and confirmed: CSRF on all mutating forms, fail-closed per-tenant model scope (no IDOR — ATS controllers `abort(404)` on cross-tenant ids), PDO prepared statements everywhere, password hashing (never logged/returned), session cookies `HttpOnly` + `SameSite=Lax` with `regenerate()` on login (anti-fixation), login rate-limit + lockout + failed-login audit, backup download path-traversal guard (`basename` + `realpath` re-check), permission-gated routes, per-tenant AES-256-GCM AI keys + prompt-injection guard, no hardcoded secrets. **Fixed two real issues:** (1) **Open redirect** — `back()` followed the `Referer` header blindly; it now only honours a same-origin (or relative) referer and otherwise falls back to `/`. (2) **CSP hardened to nonce-based** — `script-src` dropped `'unsafe-inline'` (the XSS hole) in favour of a fresh per-request `'nonce-…'`; the legitimate inline scripts (no-flash theme, install console) now carry that nonce, and the three inline `onsubmit="confirm(...)"` handlers were converted to the CSP-safe `data-confirm` pattern (app.js). Verified end-to-end in real Chromium: **0 CSP violations / 0 console errors**, and the response header nonce matches the inline-script nonce per request. Locked by `tests/Security/HardeningTest` (open-redirect guard + nonce CSP without script `unsafe-inline` + clickjacking/base-uri/object lock-downs). **Full suite 263 passing / 787 assertions.**
- **Browser QA pass (real Chromium) + two production bugs it surfaced & fixed.** Ran the full Testing & QA Bible flow in a real browser (Playwright + the pre-installed Chromium): registered a brand-new Owner, walked every page, exercised every modal/drawer/tab/dropdown/popover/switch/toast/autocomplete, and swept Desktop/Laptop/Tablet/Mobile — final run **0 console errors, 0 network errors, 0 functional/visual findings**. Two real bugs (invisible to the controller-level tests, which bypass route middleware) were found and fixed: **(1) RBAC permission drift** — `recruitment.view`/`recruitment.manage` were route-gated and present in `config/rbac.php` but never synced into the live `permissions` catalogue on already-installed systems, so `/jobs` and `/recruiter` returned **403 for everyone except the super-admin** (the whole ATS UI was unreachable). Fixed with an idempotent, self-healing re-sync (`RbacManager::resyncSystemRolePermissions()`) shipped as migration **`0046`** — the no-terminal update path that re-upserts the catalogue and re-grants every system role from config (owner `*` = all; the workspace **Administrator** now also holds recruitment). **(2) Mobile horizontal overflow** on `/design` — a closed `<details>` flyout was still laid out, and a DataGrid table defeated `overflow-x-auto` via `min-width:auto`. Fixed with an unlayered `details:not([open]) [data-flyout]{display:none}` rule (+ `max-w` on flyouts) and `min-w-0` on grid cells; document `scrollWidth` now equals the viewport at 375px on every page. Regression locked by `tests/Feature/RbacRoutePermissionTest` (route ↔ config ↔ live-catalogue parity + Owner grant). **Full suite 258 passing / 772 assertions.**
- **Frontend Design System & Component Library ([30-Frontend-Architecture](30-Frontend-Architecture.md)).** One unified design system on the existing pure-PHP/Tailwind stack — additive, backward-compatible, no redesign of shipped pages. **Design tokens** in `tailwind.config.js` (semantic colour roles `primary`/`success`/`warning`/`danger`/`info` with 50–950 scales + the legacy `brand` alias, radius, z-index layers, transitions, keyframes) mirrored as `:root` CSS variables — no hard-coded values in templates. A **38-component library** in `resources/views/components/*.php` rendered via the new `component()` helper (+ `attrs()` for safe attribute forwarding), each a self-contained partial with a Purpose/Props/States/Usage docblock that escapes every value: forms (`button`, `input`, `textarea`, `select`, `checkbox`, `radio`, `switch`, `field`, `autocomplete`, `datepicker`, `file-upload`), display (`avatar`, `badge`, `chip`, `card`, `stat`, `table`, `data-grid`, `accordion`, `breadcrumb`, `pagination`, `page-header`, `tabs`, `timeline`, `calendar`), feedback (`alert`, `toast`, `progress`, `skeleton`, `spinner`, `tooltip`), overlays (`modal`, `drawer`, `dropdown`, `popover`), search & alerts (`search`, `notification-center`), and the unified `state` (Empty/Error/Permission-denied/Offline/Loading). **Dark mode** is class-driven (`darkMode: 'class'`) with a no-flash `<head>` script, a topbar `data-theme-toggle` (persisted), and `dark:` variants on every component and the app/guest shells — one component set, two themes. **Accessibility**: visible `:focus-visible` rings, ARIA roles (`dialog`/`tablist`/`switch`/`progressbar`/`alert`), keyboard operability (Escape/Space/arrow-keys), focus management + scroll-lock for overlays. **Vanilla `app.js`** gains delegated, data-attribute behaviours (modal/drawer, tabs, switch, dismissals, dark-mode toggle, and `window.HalaToast` mounting into `#toast-region`) — progressive enhancement, no framework, no inline JS. A **living catalog** at **`GET /design`** (`DesignSystemController`, gated by `dashboard.view`) renders every component/variant/state in the real shell as always-current documentation. Verified by `tests/Feature/ComponentTest.php` (26 tests — component contracts + catalog renders 200); CSS rebuilt to `public/assets/css/app.css`. Earlier component partials (`partials/alerts.php`) are unchanged.
- **Testing & QA Bible — acceptance + isolation gates.** Acting as QA (not just dev): a new **FINAL ACCEPTANCE journey test** (`tests/Feature/AcceptanceJourneyTest`) drives the complete product flow through the real services — register a user → provision their workspace (Owner) → subscribe → create + publish a job → apply → run the AI interview (state machine) → generate the explainable report (Decision Engine) → move through the pipeline → send offer → accept → **hired** — every step asserted. A new **multi-tenant isolation test** (`tests/Security/TenantIsolationTest`) proves one workspace can never read, list or reach another's data (fail-closed `workspace_id` scope). Full QA sweep this round: **395 PHP files lint-clean, `app.js` syntax-clean, 38 components + the `/design` catalog render with 0 PHP warnings/notices, DB integrity intact (206 tables, 552 FKs, 0 ENUMs, 67 soft-delete + 166 uuid columns), full suite 254 passing / 746 assertions / 0 failures.**
- **ATS web layer (dashboard, no terminal).** Recruiter Workspace dashboard, Jobs list/create (with default pipeline)/publish/close/archive, the **Kanban pipeline board** (applications grouped by stage with a server-rendered move) and the application detail + candidate timeline — controllers in `App\Controllers\Ats`, tenant-scoped routes gated by new `recruitment.view`/`recruitment.manage` permissions, permission-gated nav, CSRF-protected forms. Verified by `tests/Feature/AtsWebTest` (every page renders 200; full suite 226 passing). See [53-ATS-Recruitment-Workflow §Web layer](53-ATS-Recruitment-Workflow.md).
- **ATS & Recruitment Workflow — the core engine ([53-ATS-Recruitment-Workflow](53-ATS-Recruitment-Workflow.md)).** The Applicant Tracking System now drives the full recruitment cycle (job → hire), with the AI Interview Engine running as interview *stages within* it. Built on the existing recruitment schema (additive migration `0045` only: `applications.assigned_recruiter_id`/`assigned_interviewer_id` + a `tasks` table — `scheduled_tasks` stays the CRON scheduler). Service layer in `App\Services\Ats`: **JobManager** (create/publish/pause/close/archive), **PipelineManager** (per-job pipelines + a seeded 11-stage default; add/reorder/clone/assign), **ApplicationFlow** (apply → move-through-stages with a `status_histories` audit row + events; assign recruiter/interviewer), **InterviewScheduler** (meetings for interviews), **OfferManager** (create→approve→send→accept[→hired]/decline), **RejectionManager** (reason+template→`rejected`+event), **TalentPool**, **AdvancedSearch** (job/status/score/source/stage/recruiter + candidate-profile attributes incl. skills/experience/availability/salary), **CandidateTimeline** (applications + status changes + interviews/meetings + notes + offers, chronological), **TaskManager** (recruiter to-dos linked to job/candidate/interview), and **AtsEvents** bridging every change into the Workflow Automation Engine ("Automation Ready"). Tenant-isolated + audited throughout; permission-gated at the controller layer. **Self-validation lifecycle test green** (Create Job→Publish→Apply→Review/Move→Assign Recruiter→Schedule Interview→Run AI Interview→Generate Report→Create Offer→Accept→Reject→Archive). **36 new tests; full suite 222 passing, 626 assertions; fresh build 56 migrations / 0 failures, 206 tables, 552 FKs, 0 ENUMs.** Pending UI (Kanban, per-role dashboards, offer PDF/e-sign, calendar export) layers on top with no schema change.
- **AI Interview Engine P5–P10 realized (migrations `0040`–`0044`).** The remaining engine, all tested offline through the P4 `AiGateway`/`FakeProvider` (no live keys): **P5** — `Orchestrator` + `MemoryEngine` (bounded summary+recent context, not full history) + `KnowledgeEngine` (grounds agents in JD/criteria/policies/blueprint) + `QualityControl` (post-response gate) [0040]. **P6** — 9 single-responsibility agents (HR/Technical/Behavior/Psychometric/Communication/Language/Culture-Fit/Risk/Decision) + `AgentRunner` that records `agent_runs` and merges weighted verdicts via the existing `DecisionEngine` into one **explainable** decision (per-agent factors) [0041]. **P8** — Anti-Cheating: `CheatingDetector` turns weighted signals into a **confidence score + band only — never proof** [0042]. **P9** — `AiObservability` (request/provider/token/cost/success metrics from `ai_requests`/`ai_responses`), `CostOptimizer` (pre-call cost estimate + cheapest viable model), `BenchmarkEngine` (compare providers per accuracy/latency/cost/quality), `SimulationMode` (dummy candidate + fake CV + generated answers, sandbox=true, writes no real data) [0043]. **P10** — `HumanReview` (every decision approve/edit/reject/request-changes with logged reason; additively extends `decision_records` with review columns), `LearningEngine` (captures human/outcome feedback within the tenant boundary), and `AiFeatures` + `config/ai_features.php` (9 AI feature flags toggled without code) [0044]. **46 new tests; full suite 182 passing, 499 assertions; fresh build 55 migrations / 0 failures, 205 tables, 547 FKs, 0 ENUMs.** Built by parallel sub-agents against the established patterns and verified centrally. See [51 §1–4,§14–20](51-AI-Interview-Engine.md).
- **Plugin SDK (extensibility foundation, migration `0039`).** A plugin system so modules add without touching core (docs/51 Extensibility Golden Rule). `plugins` is the platform registry (name/author/version/manifest/dependencies/declared-permissions/license/min-platform-version/status); `workspace_plugins` is per-tenant enablement + settings (marketplace-ready). `PluginInterface`/`AbstractPlugin` (install/enable/disable/uninstall hooks), `PluginManifest`, `PluginRegistry` (lifecycle + **dependency validation** + **version compatibility** + capability aggregation), and the **`PluginApi` sandbox** — the ONLY surface a plugin uses to extend the platform (declare permissions, menus, dashboard widgets, roles, workflow nodes, interview types, evaluation templates, reports, notifications, integrations, automation actions/conditions); a plugin never touches the DB, secrets, tenant data or AI keys directly. Includes a sample `PersonalityAssessmentPlugin`. **8 tests.**
- **Workflow Automation Engine (migration `0038`).** A Zapier/n8n-style engine inside the platform (docs/51): an `automations` rule binds a TRIGGER event to ordered CONDITION + ACTION steps; on the event the `AutomationEngine` evaluates conditions (AND) and runs actions in order, recording an `automation_runs` row + per-step `automation_run_steps` trace (the debugger / execution log). 24 trigger keys; built-in conditions (expression, always) and actions (log, set_context, webhook, notify, end); conditions/actions are registry-resolved so **plugins add their own without core edits** (Open/Closed). `AutomationBuilder` does create / publish-version / **rollback** / **clone** (`automation_versions`). Code-validated VARCHAR status (no ENUMs). **9 tests.**
- **AI Interview Engine P7 — Blueprint Library (migration `0036`) + Question Bank (migration `0037`).** **Blueprint Library:** `interview_blueprints` + `blueprint_sections` + `blueprint_section_rules` + immutable `blueprint_versions`, with a **21-role starter library** (`config/blueprints.php`: junior/senior backend, frontend, devops, AI engineer, HR, sales, business dev, marketing, PM, support, finance, legal, healthcare, education, hospitality, engineering, executive, internship, graduate, call center) — each blueprint = stages + objectives + evaluation criteria + per-section question strategy / scoring / follow-up rules / expected skills+behaviors (the editable Interview Template). `BlueprintLibrary` (instantiate) + `BlueprintEngine` (publish versions). **Question Bank:** `question_bank` + tags/taggables + reference answers + follow-ups; `QuestionBank` service supports categories/tags/difficulty/languages/job-families/source and **mixes static + AI-generated + company questions** (does not rely on AI alone). **10 tests.** (Built by sub-agents against the established patterns, verified centrally.)
- **AI Interview Engine P1+ — per-state rules for the Interview State Machine.** Each of the 21 states now carries the Bible's full rule set (docs/51 §5) — **Entry**, **Exit**, **Validation**, **Allowed Actions**, **Timeout** (minutes + on-timeout action), and **Recovery** (on-disconnect + max-resumes) — defined config-driven in `config/interview_states.php` (companies edit rules without code; a tenant may override via `interview_states.meta.rules`). `StateMachine` gained `rulesFor`/`allowedActions`/`isActionAllowed`/`entryRequirements`/`exitRequirements`/`validationRules`/`timeoutMinutes`/`onTimeout`/`recoveryFor`/`unmetEntryRequirements`/`canEnter`; the graph-legality `transition()` is unchanged (entry preconditions are enforced by the Orchestrator via `canEnter()`). +4 tests (full suite 111 passing).
- **AI Interview Engine P4 — safe model-call core realized.** The single safe entry point for every model call (no new tables — builds on the D8 `ai_providers`/`ai_models`/`tenant_ai_keys`/`ai_requests`/`ai_responses` catalog). `App\Contracts\AI\AiProvider` is the swappable provider contract; `App\Services\AI\AiGateway` runs the mandatory pipeline per request: **(1) `PromptGuard`** — injection protection on every UNTRUSTED message (detects instruction-override, role-escape, system-prompt leakage, delimiter/role-marker injection, sensitive-data probing and hidden/zero-width characters; scores risk 0–100; blocks ≥ threshold; otherwise hardens by stripping control/zero-width chars, neutralizing chat role-markers and wrapping input as untrusted data); **(2) `TokenOptimizer`** — drops empties, dedupes, bounds history while preserving the system head; **(3) `ModelRouter`** — ranks provider/model candidates from the **tenant's own keys** by default-key → preference → price; **(4) fallback** — tries each candidate in turn, switching on failure, and **audits** every attempt to `ai_requests` + `ai_responses` (all model I/O is audited; credentials never logged). The platform stores **no** keys — they are decrypted per-call from `tenant_ai_keys` (AES-256-GCM). `FakeProvider` is a deterministic offline provider that powers tests and the §19 sandbox. Per the roadmap, the core is fully testable **offline**; live HTTP adapters (OpenAI/Claude/…) plug into the registry at runtime when a workspace adds keys. **16 tests; full suite 107 passing, 246 assertions.** See [51 §12–13](51-AI-Interview-Engine.md).
- **AI Interview Engine P3 — Workflow Builder realized (migration `0035`).** A visual, no-code, no-prompt interview-logic builder modelled as a directed graph of 12 typed nodes (Start · Ask/Generate Question · Wait/Evaluate Answer · AI Analysis · If/Else condition · Generate Follow-up · Score Candidate · Generate Report · Human Approval · Finish). New tables: `interview_workflows`, `workflow_nodes` (typed via the seeded `workflow_node_type` lookup), `workflow_edges` (with branch `condition` JSON), **`workflow_versions`** (immutable published snapshot — runs execute a frozen graph), `workflow_runs` and `workflow_run_steps` (the reproducible per-interview execution trace; steps reference nodes by `node_key` against the snapshot). `App\Services\Workflow\WorkflowBuilder` validates a runnable graph (exactly one Start, ≥1 Finish, valid types, real edge endpoints, no dead-ends, all nodes reachable) and publishes versions; `WorkflowRuntime` is a start/resume **step machine** that auto-advances deterministic nodes, branches If/Else on the run context, and pauses at `await` nodes (human approval, candidate answer, or a model result injected by the future Orchestrator via `resume()`). Run/step statuses are seeded `workflow_run_status`/`workflow_step_status` lookups (no ENUMs). Per the roadmap, P3 runs with **zero model calls** — workflows are fully executable and testable today. **12 tests; full suite 91 passing, 211 assertions.** See [51 §8](51-AI-Interview-Engine.md).
- **AI Interview Engine P2 — Evaluation Templates & Decision Engine realized (migration `0034`).** The evaluation template IS the existing D9 configurable scorecard, reused to avoid a parallel scoring system (anti-duplication per [50-Continuous-Project-Audit](50-Continuous-Project-Audit.md)): `evaluation_forms` is the weighted, thresholded rubric and `evaluation_form_fields` its weighted criteria. New tables: **`evaluation_form_versions`** (immutable JSON snapshot of a rubric at publish time — the Decision Engine always scores against a frozen version), **`decision_records`** (the engine's aggregated, explainable output — overall + normalized 0–100 score, pass/fail vs threshold, recommendation; distinct from `application_decisions`, which audits pipeline moves) and **`decision_factors`** (per-criterion contribution = Explainable AI). Engines: `App\Services\Evaluation\DecisionEngine` (pure `score()` — weighted, normalized by the **sum of scored weights** so it is scale-independent and never penalizes partial scoring; clamped; recommendation bands; a factor breakdown that sums back to the overall) and `EvaluationTemplate` (validate / publish-supersede immutable versions / active snapshot / instantiate role-family starters). Role-family starter rubrics (Backend Engineer, Sales, Customer Support) in `config/evaluation_templates.php` — seeds, not behaviour. **16 tests; full suite 79 passing, 185 assertions.** See [51 §9/§3](51-AI-Interview-Engine.md).
- **AI Interview Engine P1 — Interview State Machine realized (migration `0033`).** The interview is driven by a configuration-driven state machine, not a message chain — so pause/resume/recover are just transitions. **`interview_states`** (catalog of stages: key/label/order/initial/terminal/timeout/color; 21 system stages seeded + per-tenant overrides), **`interview_state_transitions`** (append-only per-interview audit), and `interviews.state_id`. `App\Services\Interview\StateMachine` enforces legal transitions (start → initial only; terminal cannot exit; pause to `waiting`; resume; linear advance by `sort_order`; configurable `meta.allowed_next` jumps), writes the audit row and updates the interview. Config-driven (no ENUMs), tenant-scoped, idempotent. **11 tests** (legality rules + runtime persistence). See [51 §5](51-AI-Interview-Engine.md).
- **Continuous Project Audit — doc↔code drift reconciliation completed (CA-5).** Reconciled the remaining narrative docs (03/04/08–10/12/16–20/24–30/33/35–37/41–45) to the realized schema/code: tenant identifiers (`company_id`→`workspace_id`, `companies`→`workspaces`, `CompanyService/Controller`→`Workspace*`, RBAC cache key, routes), table renames (`ai_credentials`→`tenant_ai_keys`, `activity_log`→`activity_logs`, pluralized RBAC pivots), config-driven status FKs (no ENUMs), installer URLs (`/install`→`/setup`), API headers (`X-Company-Id`→`X-Workspace-Id`), and **doc 33** (`platform.diagnostics`→`system.manage`, `Controllers/Platform/DiagnosticsController`→`Controllers/System/DiagnosticsController`) to match the realized `/system` operations suite. Generic business prose, historical migration filenames and PHP class names preserved.
- **AI Interview Engine architecture spec ([51-AI-Interview-Engine](51-AI-Interview-Engine.md)).** Documentation-first (per DW-13) capture of the Enterprise AI Interview Platform: AI Orchestrator; 9 single-responsibility agents (HR/Technical/Behavior/Psychometric/Communication/Language/Culture-Fit/Risk + Decision); standalone Memory Engine (summaries/skills/contradictions/confidence-trend/timeline, bounded context — not full-history resend); **Interview State Machine** (Draft→…→Archived with entry/exit/validation/timeout/recovery rules); **Blueprint Engine + Library** + dynamic flow + follow-up intelligence; **Visual Workflow Builder** (node graph, no-code); Decision Engine; **Evaluation Templates**; Question Bank; Knowledge Engine; Model Router + Fallback + per-tenant keys; Prompt-Injection Protection + Token Optimizer + prompt versioning; AI Quality Control; Explainable AI; Anti-Cheating (confidence score, never proof); Observability Dashboard + Cost Optimizer; Human-in-the-loop; Versioning/Sandbox/Simulation/Benchmark; AI feature flags; Learning Engine + future-ready seams (RAG/vector/avatars/integrations). Includes the **D11 data-model extension** (new tables, pending ERD approval before migration — per the Database Bible), the RBAC/multi-tenant/API/security/performance impact review, and a 10-phase roadmap that starts with the user-prioritized State Machine → Evaluation Templates → Workflow Builder (phases 1–3 deliver tenant-configurable interview logic with zero model calls). Indexed in [00-README](00-README.md); extends [16](16-AI-Architecture.md)/[18](18-AI-Interview-Engine.md)/[23](23-Interview-Workflow.md).
- **Development Workflow + Continuous Project Audit Bibles adopted (governance).** Two binding process constitutions captured documentation-first: [49-Development-Workflow](49-Development-Workflow.md) (the 16-stage per-feature lifecycle, feature-completeness checklist, Definition of Done, self/regression testing, routing/DB/API/security/performance/refactoring rules, stop conditions, no fake completion) and [50-Continuous-Project-Audit](50-Continuous-Project-Audit.md) (after every phase, audit the WHOLE project — broken pages/routes/buttons, unused APIs, missing permissions, multi-tenant/RBAC/security/performance/DB/UI issues, doc↔code drift — and fix before the next phase). Indexed in [00-README](00-README.md) and cross-linked from [47](47-Enterprise-Architecture-Standards.md).
- **Setup & Installer Bible realized — no-CLI installer + operations suite.** The web installer now conforms to the [Setup Bible](32-Setup-Installer.md): a 12-step wizard at **`/setup`** (Welcome → System/Server/PHP checks with plain-language fixes → Database → Environment keys → Storage tree → Permissions + **Auto-Fix** → Mail + **Send Test Email** → AI-providers info → Super Admin → **Final Health Check**), with a **real progress bar**, live console, resume-on-failure, auto-repair of storage/permissions, and a **real final validation** (creates a workspace/user in a rolled-back transaction + storage/mail probes) gating finalize. Plus a dashboard **operations suite** (super-admin, `system.manage`, no terminal): **Diagnostics**, **Maintenance mode** (503 gate with super-admin bypass), **Backup & Restore** (pure-PHP SQL dump + ZIP, no mysqldump), **Environment Editor** (safe `.env` keys, secrets masked), and **Log Viewer** (level-coloured tail + suggested fixes). **Verified end-to-end: clean fresh install (43 migrations, health + final validation PASS, `.env` + lock written), all ops pages 200 for super-admin / 403 for others, maintenance gate + bypass + backup file confirmed, 52 tests green.** See [32-Setup-Installer](32-Setup-Installer.md) + [33-System-Diagnostics §Post-Install Operations](33-System-Diagnostics.md#post-install-operations-suite-no-terminal).
- **Full database build — blueprint realized (migrations `0018`–`0032`).** The entire approved Database Bible is now implemented and verified against MySQL/MariaDB: the D0 foundation (reference data + configuration-driven `lookup_categories`/`lookup_values` + `system_modules` registry + `permissions.module_id`/`action`), every domain D1–D10 and the D0 polymorphic shared tables (each a create migration + a companion idempotent FK migration so every table exists before any foreign key — the jobs↔pipelines cycle and all cross-domain refs are handled), the D2 support tables, the built→blueprint renames (`activity_log`→`activity_logs`, `permission_role`→`role_permissions`, `membership_role`→`membership_roles`, `user_role`→`user_roles`, `ai_credentials`→`tenant_ai_keys`), and the no-ENUM cutover (`workspaces`/`subscriptions`/`memberships`/`users` statuses and `plans.interval` → status-table / `lookup_values` FKs). Config seeds added (`ReferenceDataSeeder`, `LookupSeeder`). **Verified from scratch: 43 migrations / 0 failures, 167 tables (166 + ledger), 0 ENUM columns, 451 FKs, full reference/lookup/status seed; app test suite green; registration + dashboard flows verified.** See [database/00-Database-Bible §Migration Status](database/00-Database-Bible.md#migration-status--blueprint-realized).
- **Complete official `/docs` specification set** — the single source of truth, authored documentation-first per [00-README](00-README.md). 47 numbered documents (00–46) plus this CHANGELOG, ~13,000 lines, every file following the mandatory section template, fully cross-linked. Coverage:
  - **Overview & business:** [00-README](00-README.md), [01-Project-Vision](01-Project-Vision.md), [02-Business-Rules](02-Business-Rules.md) (stable `BR-xxx` IDs), [45-Future-Roadmap](45-Future-Roadmap.md).
  - **Architecture:** [03-System-Architecture](03-System-Architecture.md), [04-Folder-Structure](04-Folder-Structure.md), [29-API-Architecture](29-API-Architecture.md), [30-Frontend-Architecture](30-Frontend-Architecture.md), [31-Backend-Architecture](31-Backend-Architecture.md).
  - **Data:** [05-Database-Architecture](05-Database-Architecture.md), [06-ERD](06-ERD.md) (all 36 tables, built + planned), [27-Storage-System](27-Storage-System.md), [28-Search-System](28-Search-System.md).
  - **Identity & access:** [07-RBAC](07-RBAC.md), [08-Multi-Tenant](08-Multi-Tenant.md), [09-Authentication](09-Authentication.md), [10-Authorization](10-Authorization.md), [11-Permissions-Matrix](11-Permissions-Matrix.md).
  - **Company & money:** [12-Workspace-Management](12-Workspace-Management.md), [13-Subscription-System](13-Subscription-System.md), [14-Billing-System](14-Billing-System.md), [15-Payment-Gateways](15-Payment-Gateways.md).
  - **AI:** [16-AI-Architecture](16-AI-Architecture.md), [17-AI-Providers](17-AI-Providers.md), [18-AI-Interview-Engine](18-AI-Interview-Engine.md).
  - **Journeys:** [19-Candidate-Journey](19-Candidate-Journey.md), [20-Recruiter-Journey](20-Recruiter-Journey.md), [21-HR-Journey](21-HR-Journey.md), [22-SuperAdmin-Journey](22-SuperAdmin-Journey.md).
  - **Domain workflows:** [23-Interview-Workflow](23-Interview-Workflow.md), [24-Job-Lifecycle](24-Job-Lifecycle.md), [25-Application-Lifecycle](25-Application-Lifecycle.md), [26-Notification-System](26-Notification-System.md).
  - **Setup & ops:** [32-Setup-Installer](32-Setup-Installer.md), [33-System-Diagnostics](33-System-Diagnostics.md), [43-Deployment](43-Deployment.md), [44-Production-Checklist](44-Production-Checklist.md).
  - **Security & reliability:** [34-Security](34-Security.md), [35-Performance](35-Performance.md), [36-Scalability](36-Scalability.md), [37-Logging](37-Logging.md), [38-Audit-System](38-Audit-System.md).
  - **Testing & standards:** [39-Testing-Strategy](39-Testing-Strategy.md), [40-QA-Checklist](40-QA-Checklist.md), [41-Coding-Standards](41-Coding-Standards.md), [42-Code-Review-Checklist](42-Code-Review-Checklist.md).
  - **Self-audit:** [46-Architecture-Review](46-Architecture-Review.md) — independent architecture review and verification report.
  - **Enterprise standard:** [47-Enterprise-Architecture-Standards](47-Enterprise-Architecture-Standards.md) — binding layered architecture, SOLID, DI, Repository/Service/DTO, Events, Cache/Storage/Search/Queue/AI/Notification contracts, Settings, Feature Flags, Audit, Soft Delete, UUID, Policies.
- **Enterprise architecture foundation (code)**, built in tested increments against doc 47 and non-breaking to the verified Phase 1–3 flows:
  - **DI container** with reflection autowiring + interface→concrete binding; core services aliased by type.
  - **Contracts** for swappable seams: `CacheStore`, `RepositoryInterface` (+ User/Company), `EventDispatcherInterface`, `AuditLogger`.
  - **Cache layer** (`FileStore` default + `ArrayStore`); **Settings** system + **Feature flags** (per-tenant, cached, config defaults).
  - **Repository pattern** (`BaseRepository`, tenant- and soft-delete-aware) + concrete repositories; immutable **DTOs**.
  - **Soft deletes** + **RFC-4122 v4 UUIDs** on core entities (migration `0016`); audit gains `old_values`/`new_values`/`device`.
  - **Domain enums**; **domain events** (`UserRegistered`, `CompanyCreated`) + **listeners** writing the audit trail; event-driven **AuditLogger**.
  - **CompanyPolicy** + policy-aware `AccessControl` (permission + policy + tenant membership).
  - Registration refactored to a thin controller → **RegistrationService** (DTO + repository + events).
  - In-house **zero-dependency test runner** (`tests/`) with **52 passing tests** (116 assertions): container, cache, enums, settings/flags, repositories (CRUD/UUID/soft-delete/tenant scope), DTOs, events, audit (old/new), and the company policy.

### Fixed
- Added the mandatory **Security** section (per the §15 template) to [35-Performance](35-Performance.md), [36-Scalability](36-Scalability.md), and [37-Logging](37-Logging.md), surfaced by the self-audit.
- Company/membership/role/subscription rows created via service raw-inserts now generate UUIDs (previously NULL), keeping EAS-8 consistent across the Model and service write paths.

### Notes
- Documentation describes both **built** areas (verified against `app/`, `database/migrations/`, `config/`, `routes/web.php`) and **planned** areas (specified as planned, consistent with the canonical schema).
- Self-audit verification ([46-Architecture-Review](46-Architecture-Review.md)): all numbered docs contain the 14 mandatory sections; **0 broken cross-references**; permission keys, role names, and state-machine enums are consistent across the whole set.

## [1.0.0] — 2026-06-26

The first production release of HalaOps, delivering Phases 1–3: a pure-PHP,
no-framework, multi-tenant SaaS foundation with a no-CLI installer, strict
tenant isolation, a single users table with full RBAC, and platform-wide
authentication.

### Added

#### Phase 1 — Core foundation (micro-framework)
- Zero-dependency core under `app/Core/`: `Application`, `Container`, `Config`, `Env`, `Router`, `Route`, `Request`, `Response`, `Session`, `View`, `Database`, `QueryBuilder`, `Model`, `Validator`, `Hash`, `Encrypter`, `Mailer`, `Logger`, `Translator`, `Controller`.
- Custom PSR-4 autoloader and application bootstrap (`bootstrap/autoload.php`, `bootstrap/app.php`); no `vendor/`.
- Middleware pipeline with `SecurityHeaders` and `VerifyCsrfToken`, plus alias-based middleware resolution (e.g. `permission:`, `throttle:`).
- Single PDO database layer (ERRMODE_EXCEPTION, real prepared statements, utf8mb4) with nested transactions via SAVEPOINTs; fully parameterized fluent `QueryBuilder`.
- Plain-PHP view engine with layout inheritance and output escaping via `e()`.
- AES-256-GCM `Encrypter`, Argon2id `Hash` (bcrypt fallback, needs-rehash), bilingual `Translator` (AR/EN, RTL detection).
- Helper functions: `app()`, `config()`, `env()`, `view()`, `redirect()`, `url()`, `route_to()`, `e()`, `auth()`, `tenant()`, `access()`, `can()`, `__()`, `encrypt_value()`, `decrypt_value()`, and more.

#### Phase 2 — No-CLI web installer
- Browser-based installer at `/install` (`app/Controllers/Setup/InstallController.php`): requirements → database → migrate → seed → admin → finalize.
- Live AJAX install console and resume-from-last-step recovery.
- Writes `.env` and an install lock file; install gating in the application kernel so the app routes to the installer until set up.
- Migration runner and `migrations` tracking table; migrations run outside transactions (MySQL DDL auto-commit).

#### Phase 3 — Multi-tenancy, single users table, RBAC, authentication
- **Single `users` table** — no per-type tables; capability is entirely role-derived.
- **Row-level multi-tenancy** — tenant-scoped models auto-filter by `workspace_id` and **fail closed** (throw) when no tenant is active; `withoutTenantScope()` escape hatch for system/super-admin code (`app/Services/Tenancy/TenantManager.php`, `CompanyService.php`).
- **RBAC** — global permission catalogue, global and tenant roles with single-parent inheritance, policy gates (`app/Services/Rbac/AccessControl.php`, `RbacManager.php`); permissions and default tenant roles are data-driven in `config/rbac.php`.
- **Authentication** — one login/register/forgot/reset for the whole platform (`app/Controllers/Auth/*`): Argon2id hashing with transparent rehash, login throttling (5 attempts / 900s lockout), anti-enumeration password reset (hashed tokens, 60-minute TTL), session regeneration on login.
- **Company provisioning** — atomic creation of company, owner membership, default roles, owner role assignment, and a trial subscription; registrant becomes Owner.
- **Database schema** — migrations `0001`–`0015`: `users`, `companies`, `memberships`, `roles`, `permissions`, `role_permissions`, `membership_roles`, `user_roles`, `plans`, `subscriptions`, `ai_credentials`, `password_resets`, `settings`, `onboarding_progress`, `activity_log` (plus the `migrations` table). All InnoDB/utf8mb4 with real foreign keys and indexes.
- **Baseline seed data** (`database/seeders/DatabaseSeeder.php`, idempotent): full permission catalogue, the global `super-admin` role, and the single "Standard" plan (50.00 SAR, monthly, 14-day trial).
- **Tenant AI credential storage** — `ai_credentials` table holding per-tenant, AES-256-GCM-encrypted provider keys (one row per provider, `UNIQUE(workspace_id, provider)`); the platform stores no AI keys of its own.
- **Bilingual UI** — Arabic/English views and language files (`resources/lang/{en,ar}/`), full RTL/LTR support, compiled Tailwind CSS shipped at `public/assets/css/app.css` (no build step on the buyer's server).
- **Audit logging** — `activity_log` records security/business events with actor, subject, IP, and user agent.

### Security
- CSRF protection on all state-changing requests; hardened session cookies (httponly, samesite=Lax, secure on HTTPS).
- AES-256-GCM authenticated encryption for tenant secrets; Argon2id password hashing.
- Prepared statements only; output escaping in templates; security headers; rate limiting; anti-enumeration on auth flows.
- Fail-closed tenant isolation as the primary cross-tenant data-leak defense.

[Unreleased]: https://example.com/halaops/compare/v1.0.0...HEAD
[1.0.0]: https://example.com/halaops/releases/tag/v1.0.0
