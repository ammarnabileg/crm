{
  "name": "halaops",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for HalaOps front-end assets (development only — the shipped package contains pre-compiled CSS and requires no build step on the server).",
  "scripts": {
    "build:css": "tailwindcss -i ./resources/css/app.css -o ./public/assets/css/app.css --minify",
    "watch:css": "tailwindcss -i ./resources/css/app.css -o ./public/assets/css/app.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
